# Public Assets

Files in this directory are served from the deployed site root.

- `app-ads.txt` must contain the production Google AdMob publisher line before launch.
- `fonts/` contains the Outfit font files reused from the iOS app.
